
#ifndef I_REIMU_GAP_H
#define I_REIMU_GAP_H

#include "../lib/defs.glslh"

#define NUM_GAPS 4

#ifdef VERT_STAGE
ATTRIBUTE(0) vec3 position;
ATTRIBUTE(2) vec2 texCoordRawIn;

ATTRIBUTE(3) vec4 gapCoords;
ATTRIBUTE(4) vec2 gapAngles;
#endif

#ifdef FRAG_STAGE
OUT(0) vec4 fragColor;
#endif

UNIFORM(0) sampler2D tex;
UNIFORM(1) float time;
UNIFORM(2) vec2 viewport;
UNIFORM(3) vec2 gap_size;

VARYING(0) vec2 texCoord;
VARYING(1) vec2 texCoordGapView;

#endif
